#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/node$exe" // line tells os to execute the code using node.js interpreter (SHEBANG) "$basedir/../aa24_simple_calculator/index.js" $args
  } else {
    & "$basedir/node$exe" // line tells os to execute the code using node.js interpreter (SHEBANG) "$basedir/../aa24_simple_calculator/index.js" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "node$exe" // line tells os to execute the code using node.js interpreter (SHEBANG) "$basedir/../aa24_simple_calculator/index.js" $args
  } else {
    & "node$exe" // line tells os to execute the code using node.js interpreter (SHEBANG) "$basedir/../aa24_simple_calculator/index.js" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
